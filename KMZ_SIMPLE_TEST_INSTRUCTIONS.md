# Інструкції для простого тестування KMZ імпорту

## Проблема
При відкритті `http://localhost:8002/test-kmz-import.html` виникає помилка `Uncaught ReferenceError: L is not defined` через проблеми з завантаженням Leaflet.

## Рішення
Створено простий тестовий файл без складних залежностей для перевірки KMZ імпорту.

## Тестування

### Крок 1: Відкрийте простий тестовий файл
```
http://localhost:8002/test-kmz-simple.html
```

### Крок 2: Перевірте завантаження
- Карта повинна відобразитися без помилок
- В консолі повинно бути повідомлення: "Простий тестовий скрипт KMZ імпорту завантажено"

### Крок 3: Імпортуйте KMZ файл
1. Натисніть "Вибрати файл" і виберіть KMZ файл
2. Натисніть "Імпортувати KMZ"
3. Дочекайтеся завершення імпорту

### Крок 4: Перевірте результати
В консолі браузера повинні з'явитися логи:
- "Починаємо обробку KMZ файлу: [назва файлу]"
- "Знайдено KML файл: [назва файлу]"
- "Назва шару: [назва шару]"
- "Знайдено placemarks: [кількість]"
- "Створено об'єкт [номер]: {...}"
- "KMZ файл успішно імпортовано!"
- "Кількість об'єктів: [кількість]"

### Крок 5: Перевірте відображення
- Об'єкти повинні з'явитися на карті
- Карта повинна автоматично центруватися на об'єктах
- Внизу сторінки повинен з'явитися детальний звіт про імпорт

### Крок 6: Перевірте структуру об'єктів
В звіті кожен об'єкт повинен мати:
- ✅ Тип (Marker, Polyline, Polygon)
- ✅ Назва (з KML файлу)
- ✅ Опис (з KML файлу)
- ✅ Має feature: true
- ✅ Має properties: true
- ✅ Feature properties: {...} (JSON з метаданими)

## Очікуваний результат
Після імпорту KMZ файлу:
- ✅ Карта відображається без помилок
- ✅ Об'єкти імпортуються з іменами та описами
- ✅ Feature об'єкти створюються з правильними properties
- ✅ Детальний звіт показує всі метадані
- ✅ В консолі немає помилок

## Діагностика

### Якщо карта не відображається:
1. Перевірте, чи завантажилася бібліотека Leaflet
2. Перевірте консоль на помилки
3. Перевірте, чи є елемент з id="map"

### Якщо об'єкти не імпортуються:
1. Перевірте, чи є KML файл в KMZ архіві
2. Перевірте, чи правильно парсяться координати
3. Перевірте, чи є placemarks в KML файлі

### Якщо метадані відсутні:
1. Перевірте, чи є теги `<name>` та `<description>` в KML
2. Перевірте, чи створюються feature об'єкти
3. Перевірте, чи правильно копіюються properties

## Порівняння з основним файлом
Простий тестовий файл:
- ✅ Не залежить від складних модулів
- ✅ Використовує тільки Leaflet та JSZip
- ✅ Легко діагностувати проблеми
- ✅ Показує детальну інформацію про імпорт

Основний файл:
- ❌ Залежить від багатьох модулів
- ❌ Складна ініціалізація
- ❌ Важко діагностувати проблеми
- ❌ Потребує виправлення завантаження Leaflet 